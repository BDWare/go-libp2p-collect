// Package relaypsc implements relayable PubSubCollector, which collects the 
// responses around the overlay network via a spinning tree.
// The responses are high-efficiently deduplicated by stream processing.
package relaypsc